# CPAP — minimal runtime dependencies
# Tested with Python 3.12 on Windows (CUDA 12.1). A CUDA-capable GPU is required.
#
# Install PyTorch separately so the build matches your CUDA version, e.g.:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# then:
#   pip install -r requirements.txt

torch==2.5.1            # GPU build (cu121) recommended; see note above
transformers>=4.30,<5  # BERT / RoBERTa text encoders (downloaded from the HuggingFace Hub)
numpy==1.26.4          # keep <2.0 for compatibility with the cached feature pickles
pandas==2.2.2
scikit-learn==1.4.2    # KMeans confounder dictionary + evaluation metrics
tqdm==4.66.4
matplotlib==3.8.4      # live loss / APF plots
thop>=0.1.1            # FLOPs / params profiling in solver.py
